import React from 'react';
import classes from '@/styles/Register.module.css'

function register(props) {
    return (
        <h1 className={classes.button}>Register page</h1>
    );
}

export default register;